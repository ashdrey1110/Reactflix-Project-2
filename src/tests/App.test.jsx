// import App from '../App';

// test('Does it import?', () => {
//   expect(App).toBeDefined();
// });

test('Two + Two = 4!!!', () => {
    expect(2 + 2).toBe(4);
  });

test('Two - Two = 0!!!', () => {
expect(2 - 2).toBe(0);
});

